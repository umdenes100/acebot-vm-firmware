# Copy this file to config.py and fill in your credentials.

# Wi‑Fi
WIFI_SSID = "YOUR_SSID"
WIFI_PASSWORD = "YOUR_PASSWORD"

# Vision system WebSocket URL, e.g. "ws://192.168.4.1:8765/ws"
WS_URL = "ws://HOST:PORT/path"

# Debug logging (library prints). Student-facing prints via enes100.print() are unaffected.
DEBUG = False

# Optional: spoof MAC address (bytes) or None to disable. Example: b"\xaa\xbb\xcc\xdd\xee\xff"
MAC_SPOOF = None
